export const API_BASE = "http://localhost:4000/api";
// If this changes (e.g. a real deployed domain), also update the
// themeBridge.js content_scripts "matches" entry in manifest.json - it's a
// static match pattern and can't reference this constant.
export const WEB_BASE = "http://localhost:5173";
